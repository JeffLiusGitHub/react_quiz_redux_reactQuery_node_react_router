import '../App.css';

function Menu() {
	return (
		<div className="Menu">
			<label>Enter Your Name:</label>
			<input
				type="text"
				placeholder="Ex. John Smith"
				onChange={(event) => {

				}}
			/>
			<button
				onClick={() => {
				}}
			>
				Start Quiz
			</button>
		</div>
	);
}

export default Menu;
